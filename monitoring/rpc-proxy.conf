worker_processes 1;
events { worker_connections 64; }

http {
    access_log off;
    server_tokens off;

    server {
        listen 18081;

        location / {
            proxy_pass http://monerod:18081;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
        }
    }
}
